# 大标题（说明操作内容 比如-任务）

## 中标题（说明操作 比如 xxx平台-添加）

> 表明操作的URL

*请求的方式：XXX*

如果有参数（选择性填写）

| 参数名 | 值   | 必要性 | 作用 |
| ------ | ---- | ------ | ---- |
|        |      |        |      |

请求头需要带上的cookie（选择性填写）

| 字段 | 值   | 必要性 | 作用 |
| ---- | ---- | ------ | ---- |
|      |      |        |      |

*请求体：XXX*

```
表单、JSON、XML 的抓包原始内容
```

*请求体字段说明*

| 字段 | 值   | 必要性 | 作用 |
| ---- | ---- | ------ | ---- |
|      |      |        |      |

*响应体：XXX*

**失败响应**

```
表单、JSON、XML 的抓包原始内容
```

*失败响应体体字段说明*

| 字段名 | 值   | 作用 |
| ------ | ---- | ---- |
|        |      |      |

**成功响应**

```
表单、JSON、XML 的抓包原始内容
```

*成功响应体体字段说明*

| 字段名 | 值   | 作用 |
| ------ | ---- | ---- |
|        |      |      |

如果有状态码的改变这里需要注明
